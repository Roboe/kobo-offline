{
  "name": "kobo-offline",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "nakedjsx": "^0.14.2",
    "node-fetch": "^3.3.2",
    "prop-types": "^15.8.1",
    "sass": "^1.69.3"
  },
  "devDependencies": {
    "prettier": "^2.3.1"
  },
  "scripts": {
    "cache:dicts": "node scripts/download-all-dictionaries.mjs",
    "clean-build": "rm -rf build && cp -rf public build",
    "predev": "npm run clean-build",
    "dev": "nakedjsx pages --pretty --dev",
    "prebuild": "npm run clean-build",
    "build": "nakedjsx pages"
  }
}
